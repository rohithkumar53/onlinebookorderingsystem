export const addToCart=(book, bookCopies)=> dispatch =>{

    const item={
        name: book.name,
        stock: book.stock,
        copies: bookCopies,
        _id: book._id,
        image: book.image
    }
    dispatch({type: 'ADD_TO_CART', payload: item});
}